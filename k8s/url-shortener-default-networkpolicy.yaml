apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: url-shortener-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/url-shortener-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/url-shortener-default: "true"
